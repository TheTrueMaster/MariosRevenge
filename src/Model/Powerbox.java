package Model;

import java.awt.Image;

public class Powerbox extends Box{

	public Powerbox(int x, int y, Image icon) {
		super(x, y, icon);
	}
	
	/* public Powerup getPowerup(Player p)
	{
		if (p.getHealth() == 2)
		{
			//return(new Fireflower(x, y + a, icon);
		}
		
		else if (p.getHealth() == 1)
		{
			//return (new Mushroom(x, y + a, icon);
		}
		
	} */


}

